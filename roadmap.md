# Roadmap: Linux Admin<br>21 сентября – 27 ноября 2026

*Режим: 2 часа в будни (45 мин теория + 1 час 15 мин практика)*

### День 1: 21 сентября (Понедельник) — Развертывание среды и Git
*   Теория
    *   Что такое виртуализация и чем отличается сеть «Сетевой мост» (Bridged) от NAT;
    *   Основы Git: что такое репозиторий, коммит, ветка, remote;
    *   Почему для Git лучше использовать SSH-ключи, а не пароли.
*   Практика
    1. Установить VirtualBox и создать ВМ с Ubuntu Server 22.04;
    2. В настройках ВМ установить тип подключения сети: **Сетевой мост (Bridged Adapter)**;
    3. Узнать IP сервера командой `ip a` или `hostname -I` и подключиться к нему через MobaXterm (SSH);
    4. Установить и настроить Git;
    5. Сгенерировать и добавить SSH-ключ на GitHub;
    6. Инициализировать репозиторий и сделать первый push.
*   Цели
    * Сервер доступен по SSH;
    * Git настроен через SSH-ключ;
    * Первый коммит на GitHub.
*   Ссылки
    *   VirtualBox: [Скачать](https://download.virtualbox.org/virtualbox/7.2.18/VirtualBox-7.2.18-175117-Win.exe)
    *   Ubuntu Server 22.04: [Скачать ISO](https://releases.ubuntu.com/jammy/ubuntu-22.04.5-live-server-amd64.iso)
    *   MobaXterm: [Скачать Home Edition](https://mobaxterm.mobatek.net/download-home-edition.html)

---

### День 2: 22 сентября (Вторник) — Файловая система и пакеты
*   Теория
    *   Стандартная иерархия файловой системы Linux: `/`, `/etc`, `/var`, `/home`, `/tmp`, `/bin`;
    *   Абсолютные (`/etc/nginx`) и относительные (`../nginx`) пути;
    *   Как работает `apt`: разница между `update` (обновление списка) и `upgrade` (обновление пакетов).
*   Практика
    1. Создать вложенную структуру папок одной командой:
       ```bash
       mkdir -p ~/projects/web/{html,logs,scripts}
       ```
    2. Установить утилиты для мониторинга и навигации:
       ```bash
       sudo apt install htop curl tree -y
       ```
    3. Визуализировать созданную структуру:
       ```bash
       tree ~/projects
       ```
    4. Потренировать перемещение и копирование:
       ```bash
       cp ~/projects/web/logs ~/projects/web/logs_backup
       mv ~/projects/web/logs_backup ~/projects/web/archive_logs
       rm -rf ~/projects/web/archive_logs
       ```
    5. Запустить `htop`, изучить загрузку CPU/RAM, выйти через клавишу `F10` или `q`.
*   Цели
    *   Понимание структуры файловой системы Linux;
    *   Умение устанавливать пакеты через `apt`;
    *   Практика работы с файлами и директориями.
*   Ссылки
    *   Документация Ubuntu по структуре ФС: [Filesystem Hierarchy Standard](https://help.ubuntu.com/community/LinuxFilesystemTreeOverview)

---

### День 3: 23 сентября (Среда) — Потоки, перенаправление и редакторы
*   Теория
    *   Стандартные потоки: `stdin` (0), `stdout` (1), `stderr` (2);
    *   Операторы перенаправления: `>` (перезаписать), `>>` (дописать), `|` (передать вывод одной команды на вход другой);
    *   Базовые команды редактора `nano` (в отличие от сложного `vim`).
*   Практика
    1. Создать и отредактировать файл в `nano`:
       ```bash
       nano ~/projects/web/scripts/info.txt
       ```
       Написать 3 строки текста. Сохранить: `Ctrl+O` -> `Enter` -> `Ctrl+X`.
    2. Перенаправить вывод команды в файл (перезапись):
       ```bash
       ls -la ~/projects > ~/projects/web/logs/list.txt
       ```
    3. Дописать строку в конец файла без перезаписи:
       ```bash
       echo "Проверка добавления новой строки" >> ~/projects/web/logs/list.txt
       ```
    4. Скачать реальный тестовый лог-файл для будущих тренировок:
       ```bash
       curl -o ~/projects/web/logs/nginx.log https://raw.githubusercontent.com/elastic/examples/master/Common%20Data%20Formats/nginx_logs/nginx_logs
       ```
*   Цели
    *   Понимание стандартных потоков ввода-вывода;
    *   Умение перенаправлять вывод команд в файлы;
    *   Базовая работа в текстовом редакторе `nano`.
*   Ссылки
    *   Документация GNU по перенаправлению: [Redirections](https://www.gnu.org/software/bash/manual/html_node/Redirections.html)
    *   Шпаргалка по nano: [Nano Editor Cheat Sheet](https://www.nano-editor.org/dist/latest/cheatsheet.html)

---

### День 4: 24 сентября (Четверг) — Поиск и фильтрация данных
*   Теория
    *   Как работает `grep` (поиск по содержимому), `find` (поиск по имени/атрибутам файлов), `wc` (подсчет строк/слов);
    *   Флаги `grep`: `-i` (игнорировать регистр), `-v` (инвертировать поиск), `-c` (подсчет совпадений).
*   Практика
    1. Найти все строки с ошибкой 404 и вывести на экран:
       ```bash
       grep " 404 " ~/projects/web/logs/nginx.log
       ```
    2. Сохранить только эти ошибки в новый файл:
       ```bash
       grep " 404 " ~/projects/web/logs/nginx.log > ~/projects/web/logs/errors_404.txt
       ```
    3. Посчитать количество строк в этом новом файле:
       ```bash
       wc -l ~/projects/web/logs/errors_404.txt
       ```
    4. Найти все `.txt` файлы в папке `projects` и вложенных папках:
       ```bash
       find ~/projects -name "*.txt"
       ```
    5. Объединить команды через pipe (найти ошибки и сразу посчитать их, без создания промежуточного файла):
       ```bash
       grep " 404 " ~/projects/web/logs/nginx.log | wc -l
       ```
*   Цели
    *   Умение искать информацию в логах с помощью `grep`;
    *   Понимание работы конвейеров (pipes);
    *   Практика анализа системных логов.
*   Ссылки
    *   Документация GNU grep: [GNU Grep Manual](https://www.gnu.org/software/grep/manual/grep.html)
    *   Документация GNU find: [GNU Find Manual](https://www.gnu.org/software/findutils/manual/html_node/find_html/)

---

### День 5: 25 сентября (Пятница) — Процессы, сервисы и закрытие недели
*   Теория
    *   Жизненный цикл процесса в Linux;
    *   Разница между `ps` (мгновенный снимок) и `top`/`htop` (динамический просмотр);
    *   Основы `systemd`: что такое юнит, как посмотреть статус службы.
*   Практика
    1. Посмотреть все запущенные процессы и отфильтровать ssh:
       ```bash
       ps aux | grep ssh
       ```
    2. Проверить статус SSH-сервиса через systemd:
       ```bash
       sudo systemctl status ssh
       ```
    3. Посмотреть историю своих коммитов в красивом виде:
       ```bash
       git log --oneline --graph
       ```
    4. Открыть `week1_notes.md` через `nano`, исправить версию Ubuntu на актуальную, проверить орфографию, добавить раздел "Итоги недели".
    5. Зайти на GitHub через браузер, проверить, что репозиторий выглядит аккуратно и профессионально.
*   Цели
    *   Понимание управления процессами в Linux;
    *   Умение работать с `systemd` для управления сервисами;
    *   Оформленный README и чистая история коммитов.
*   Ссылки
    *   Документация systemd: [systemd for Administrators](https://www.freedesktop.org/software/systemd/man/systemd.html)
    *   Conventional Commits: [Conventional Commits Specification](https://www.conventionalcommits.org/en/v1.0.0/)

---

**Неделя 2 (28 сентября – 2 октября): Пользователи, права доступа и SSH**
*   **Задача:** Понять, как устроена безопасность в Linux.
*   **Практика:** Создать нового пользователя. Сгенерировать SSH-ключи на основном ПК (`ssh-keygen`). Настроить вход на сервер *только* по ключам, полностью отключив вход по паролю в `/etc/ssh/sshd_config`.
*   **Темы:** Пользователи и группы (`useradd`, `groupadd`), права доступа (`chmod`, `chown`), утилита `sudo`.
*   🎯 **Для резюме:** В блок "Навыки" добавляем: *"Настройка безопасного удаленного доступа (SSH keys), управление правами доступа (chmod, chown, sudo)"*.

**Неделя 3 (5–9 октября): Работа с текстом и редакторы**
*   **Задача:** Научиться быстро искать информацию в логах (это 50% работы админа).
*   **Практика:** Скачать большой текстовый лог-файл (например, access.log Nginx). Найти все строки с ошибкой "404" или "500", посчитать их количество и записать результат в новый файл `errors.txt`.
*   **Темы:** Vim (базовые команды: `i`, `:wq`, `:q!`), утилиты `grep`, `awk`, `sed`, `less`, `tail -f`.
*   🎯 **Для резюме:** В блок "Навыки" добавляем: *"Анализ системных логов с помощью grep, awk, sed, tail"*.

**Неделя 4 (12–16 октября): Процессы и системные службы (Systemd)**
*   **Задача:** Понять, как Linux управляет программами.
*   **Практика:** Написать простой bash-скрипт (например, вывод времени и загрузки CPU в файл). Создать для него кастомный юнит `.service` в systemd. Настроить автозапуск, научиться делать `restart`, `status` и читать логи через `journalctl -u myservice`.
*   **Темы:** Жизненный цикл процесса (`ps`, `top`, `kill`), архитектура `systemd`.
*   🎯 **Для GitHub:** Залить этот `.service` файл и скрипт в репозиторий с инструкцией, как его установить. Сделать коммит.

**Неделя 5 (19–23 октября): Сетевая подсистема Linux**
*   **Задача:** Привязать твои знания из МТС/МТУСИ к практике в Linux.
*   **Практика:** Изменить динамический IP сервера на статический через `netplan`. Настроить базовый брандмауэр (`ufw`), закрыв все порты, кроме SSH (22) и HTTP (80).
*   **Темы:** Утилиты `ip a`, `ip route`, `ss -tlnp`, `ping`, `traceroute`, `tcpdump` (базово), диагностика DNS через `dig` и `nslookup`.
*   🎯 **Для резюме:** В блок "Навыки" добавляем: *"Настройка сетевых интерфейсов (netplan), базовая диагностика сетей (tcpdump, ss, dig), настройка ufw"*.

---

#### 📅 ЭТАП 2: Сервисы, Автоматизация и Выход на Рынок (Ноябрь)

**Неделя 6 (26–30 октября): Веб-серверы и Базы данных**
*   **Задача:** Развернуть классический стек.
*   **Практика:** Установить Nginx и PostgreSQL. Настроить Nginx так, чтобы он отдавал твою кастомную HTML-страницу ("Привет, это сервер Ивана"). Зайти в консоль PostgreSQL, создать базу данных и пользователя.
*   **Темы:** Конфигурирование Nginx (блоки `server`, `location`), базовые команды SQL в консоли (`\l`, `\c`, `CREATE USER`).
*   🎯 **Для резюме:** В блок "Навыки" добавляем: *"Базовое администрирование Nginx и PostgreSQL"*.

**Неделя 7 (2–6 ноября): Автоматизация на Bash / Cron + Ansible (введение)**
*   **Задача:** Заставить сервер работать за тебя. Это твой главный пет-проект.
*   **Практика:**
    *   **Bash/Cron:** Написать Bash-скрипт, который каждую ночь делает резервную копию папки `/etc/nginx`, упаковывает её в `.tar.gz` архив с датой в названии и складывает в папку `/backup`. Добавить этот скрипт в `crontab`.
    *   **Ansible:** Установить Ansible на свой основной ПК (или на сервер). Написать первый `playbook`, который устанавливает Nginx на твой Ubuntu Server. Запустить его.
*   **Темы:** Переменные, циклы и условия в Bash, синтаксис `crontab`. Основы Ansible: `inventory`, `playbook`, модули `apt`, `service`, `copy`.
*   🎯 **Для GitHub:** Залить скрипт бэкапа и Ansible-playbook. Написать крутой `README.md`: *"Скрипт для автоматизации бэкапа конфигураций Nginx с ротацией по дате + Ansible-playbook для установки Nginx"*. **Это будет твой козырь на собеседовании.**

**Неделя 8 (9–13 ноября): Docker и Мониторинг (Zabbix)**
*   **Задача:** Перестать бояться контейнеров и научиться следить за инфраструктурой.
*   **Практика:**
    *   **Docker:** Установить Docker. Написать простейший `Dockerfile` для своего Python-скрипта (из прошлых опытов) или для Nginx, собрать образ (`docker build`) и запустить его (`docker run`), пробросив порты.
    *   **Zabbix:** Установить Zabbix-сервер и агент на свой Ubuntu Server (можно в Docker). Настроить базовые метрики (CPU, RAM, диск) и посмотреть, как выглядят алерты.
*   **Темы:** `docker run`, `docker ps`, `docker logs`, базовый `docker-compose`. Основы Zabbix: установка, веб-интерфейс, метрики, триггеры.
*   🎯 **Для резюме:** В блок "Навыки" добавляем: *"Понимание основ контейнеризации (Dockerfile, docker run), базовый мониторинг (Zabbix)"*.

**Неделя 9 (16–20 ноября): Закрепление и Траблшутинг**
*   **Задача:** Научиться чинить то, что сломалось (имитация собеседования).
*   **Практика:** Идти на сайт **SadServers.com** (или аналоги) и пройти 3-5 сценариев. Пытаться понять, почему не работает сеть, почему переполнен диск (`df -h`, `du -sh`) и почему падает сервис.
*   🎯 **Для резюме:** В раздел "Обо мне" добавляем фразу: *"Регулярно решаю практические задачи по траблшутингу Linux-серверов на платформе SadServers для поддержания навыков"*. (HR это обожают).

**Неделя 10 (23–27 ноября): Финальный проект, Резюме и старт откликов**
*   **Задача:** Конвертировать знания в приглашения.
*   **Практика:**
    1.  **Финальный проект:** Собери всё воедино в репозитории `my-linux-infra`. Внутри: Ansible-playbook для развертывания Nginx + PostgreSQL, `docker-compose.yml` для запуска сервисов, конфиги Zabbix. В `README.md` подробно распиши, что это за проект и как его развернуть.
    2.  **Резюме:** Обновить резюме на HH.ru, внедрив все пункты "🎯 Для резюме" из этого плана. В разделе "Проекты" со ссылкой на GitHub опиши свой финальный проект.
    3.  **Опыт работы:** Исправить опыт работы на реальные ~2 года 3 месяца (если это соответствует действительности).
    4.  **Отклики:** Начать рассылать отклики на вакансии "Младший системный администратор Linux", "Инженер технической поддержки 2-й линии", "Junior DevOps" со статусом "Готов выйти в начале декабря".
